class HenchComplex {
  hench;
  henchLevel;

  constructor(hench, henchLevel) {
    this.hench = hench;
    this.henchLevel = henchLevel;
  }

  setHench(hench) {
    this.hench = hench;
  }
}

export { HenchComplex };
